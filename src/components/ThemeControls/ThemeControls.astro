---
/**
 * ThemeControls - Astro wrapper for theme control islands
 * Loads each control as a separate island for better code splitting
 */
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;

import LanguageDropdown from "./LanguageDropdown";
import DarkLightToggle from "./DarkLightToggle";
import AccentPicker from "./AccentPicker";
---

<div
  class:list={[
    "relative flex items-center gap-1.5",
    "lg:fixed lg:top-1/2 lg:left-1/2 lg:-translate-x-1/2 lg:-translate-y-1/2",
    "transition-opacity duration-300 ease-in-out z-999999",
    "opacity-100 pointer-events-auto",
    className,
  ]}
>
  <LanguageDropdown client:load />
  <DarkLightToggle client:load />
  <AccentPicker client:load />
</div>
